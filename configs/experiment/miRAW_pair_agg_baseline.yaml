# @package _global_
# miRAW pair-level aggregator baseline. Inherits pair_agg_core (the canonical body, which
# keeps miRAW's data/name/wandb as defaults); only re-adds the per-dataset pair cache_root
# that was factored out of the base. Composed result unchanged (cfgdiff = 0).
defaults:
  - _base/pair_agg_core
  - _self_

data:
  pair:
    cache_root: pair_cache/miRAW_TargetNet_Optimized_dp-0.1_Test_1-5_Test_0,6-9
