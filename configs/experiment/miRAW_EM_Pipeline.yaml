# @package _global_
# miRAW EM pipeline — now a thin alias of the shared base _base/em_core
# (em_core is the byte copy of the original miRAW_EM_Pipeline body, which is the
# canonical EM configuration). Composed result is unchanged vs the pre-refactor
# snapshot (verified by tools/refactor_verify, cfgdiff = 0).
defaults:
  - _base/em_core
  - _self_
