# @package _global_
# deepTargetPro window-level TargetNet_Optimized — thin alias of _base/window_tnopt_core
# (byte copy of this config's original body, the canonical window-level TN-Opt baseline).
# Composed result unchanged (cfgdiff = 0).
defaults:
  - _base/window_tnopt_core
  - _self_
