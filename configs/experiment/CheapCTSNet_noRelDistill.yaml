# @package _global_
# Ablation: relational distillation off. Inherits cheap_core, overrides only the 2 keys
# the snapshot diff showed differ (experiment.name, run.distill_beta_rel 1 -> 0).
defaults:
  - _base/cheap_core
  - _self_

experiment:
  name: CheapCTSNet_noRelDistill

run:
  distill_beta_rel: 0
