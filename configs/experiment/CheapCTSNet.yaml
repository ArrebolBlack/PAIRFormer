# @package _global_
# CheapCTSNet distillation — thin alias of _base/cheap_core (byte copy of the original
# canonical CheapCTSNet body). Composed result unchanged (cfgdiff = 0).
defaults:
  - _base/cheap_core
  - _self_
