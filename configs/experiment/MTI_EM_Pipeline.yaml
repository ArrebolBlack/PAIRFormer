# @package _global_
# MTI EM pipeline = mti_em_core + bench params. Composes identically to the original
# MTI_EM_Pipeline (cfgdiff = 0).
defaults:
  - _base/mti_em_core
  - _base/em_bench
  - _self_
